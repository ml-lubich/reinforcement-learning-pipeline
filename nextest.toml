# https://nexte.st/
# Install: cargo install cargo-nextest --locked
# Run:     cargo nextest run
#
# Optional; `cargo test` is the default supported path.

[profile.default]
slow-timeout = { period = "60s", terminate-after = 2 }
